<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="com.kb.crunchit.mapper.UserMapper">
    <select id="findByEmail" resultType="User" parameterType="string">
        select *
        from user
        where email = #{email};
    </select>

    <select id="findByNickname" resultType="User" parameterType="string">
        select *
        from user
        where nickname = #{nickname};
    </select>
    <select id="getUserProfile" resultType="string" parameterType="string">
        select profile_url
        from user
        where email = #{email};
    </select>

    <insert id="insertUser" parameterType="User">
        insert into user (nickname, password, phone_number, email, birth)
        values (#{nickname}, #{password}, #{phone_number}, #{email}, #{birth})
    </insert>

    <update id="updateUser" parameterType="User">
        update user
        set nickname = #{nickname}, birth = #{birth}, phone_number = #{phone_number}
        where email = #{email}
    </update>
    <update id="updateUserProfile" parameterType="User">
        update user
        set profile_url = #{profile_url}
        where email = #{email}
    </update>

</mapper>
